<g:select name="niveauId" value="${niveauId}"
                      noSelection="${['null': 'Indéterminé']}"
                      from="${niveaux}"
                      optionKey="id"
                      optionValue="libelleLong"/>